<div class="container mt-5">
    <h1 class="text-center mb-5">{{type}}</h1>
    <ul class="list-group">
        {{#each ngo}}
        <li class="list-group-item text-center"><a href="/ngo/{{_id}}">{{name}}</a></li>
        {{/each}}
    </ul>
</div>